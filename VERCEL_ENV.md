Required Vercel Environment Variables

- EXPO_PUBLIC_APPWRITE_ENDPOINT: The Appwrite API endpoint (e.g., https://nyc.cloud.appwrite.io/v1)
- EXPO_PUBLIC_APPWRITE_PROJECT_ID: The Appwrite project ID

Set these in the Vercel project settings -> Environment Variables so the static build can access them at build time.
